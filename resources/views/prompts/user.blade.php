Translate the following content items from {{ $sourceLocaleName }} to {{ $targetLocaleName }}.

Each item has an `id` (a path identifier — do not translate) and a `text` (the content to translate).
Return a JSON array with the same `id` values and translated `text` values.
